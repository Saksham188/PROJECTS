# Retrieval Benchmark

| Query | Strategy A (Raw Vector Search) | Strategy B (Expanded Query) |
| --- | --- | --- |
| How does the system handle peak load? | Message queues decouple producers from consumers. During peak load, the queue grows while consumers scale out to drain it, smoothing spikes and protecting downstream services from overload.<br>Graceful degradation keeps core features available by disabling non-critical components. For example, recommendations might be turned off during peak load to preserve checkout performance.<br>Database sharding distributes data across nodes to increase write throughput. When shards become hot, rebalancing or adding shards distributes peak load more evenly. | Autoscaling in distributed systems relies on monitoring CPU, memory, and request latency to add or remove instances. When traffic spikes, the orchestrator increases replica counts and rebalances traffic through the load balancer to prevent saturation.<br>Database sharding distributes data across nodes to increase write throughput. When shards become hot, rebalancing or adding shards distributes peak load more evenly.<br>Caching layers such as Redis or CDN edge nodes absorb repeated reads during peak hours. By reducing database hits, caches lower tail latency and help the system maintain throughput under bursty load. |
| What protects services from cascading failures during traffic spikes? | Circuit breakers and rate limiters protect critical services from cascading failures. When upstream traffic exceeds safe thresholds, the system sheds load gracefully and prevents resource exhaustion.<br>Autoscaling in distributed systems relies on monitoring CPU, memory, and request latency to add or remove instances. When traffic spikes, the orchestrator increases replica counts and rebalances traffic through the load balancer to prevent saturation.<br>Message queues decouple producers from consumers. During peak load, the queue grows while consumers scale out to drain it, smoothing spikes and protecting downstream services from overload. | Circuit breakers and rate limiters protect critical services from cascading failures. When upstream traffic exceeds safe thresholds, the system sheds load gracefully and prevents resource exhaustion.<br>Autoscaling in distributed systems relies on monitoring CPU, memory, and request latency to add or remove instances. When traffic spikes, the orchestrator increases replica counts and rebalances traffic through the load balancer to prevent saturation.<br>Message queues decouple producers from consumers. During peak load, the queue grows while consumers scale out to drain it, smoothing spikes and protecting downstream services from overload. |
| How do caches help keep latency low at scale? | Caching layers such as Redis or CDN edge nodes absorb repeated reads during peak hours. By reducing database hits, caches lower tail latency and help the system maintain throughput under bursty load.<br>Observability stacks combine metrics, logs, and traces. During peak load, dashboards highlight saturation, error rates, and p99 latency so operators can scale and tune bottlenecks quickly.<br>Message queues decouple producers from consumers. During peak load, the queue grows while consumers scale out to drain it, smoothing spikes and protecting downstream services from overload. | Caching layers such as Redis or CDN edge nodes absorb repeated reads during peak hours. By reducing database hits, caches lower tail latency and help the system maintain throughput under bursty load.<br>Observability stacks combine metrics, logs, and traces. During peak load, dashboards highlight saturation, error rates, and p99 latency so operators can scale and tune bottlenecks quickly.<br>Batch processing pipelines often run during off-peak windows to reduce contention with online traffic. Job schedulers can throttle heavy tasks when latency SLOs are at risk. |